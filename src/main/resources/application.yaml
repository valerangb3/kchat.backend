ktor:
    application:
        modules:
            - com.kchat.ApplicationKt.module
    deployment:
        port: 8080
postgres:
  url: "jdbc:postgresql://localhost/default"
  user: username
  #password: passwordjwt:
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"

jwt:
  secret: "secret"
  issuer: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"